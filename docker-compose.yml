services:
  # ---------------------------------------------------------------------------
  # PostgreSQL – primary data store
  # ---------------------------------------------------------------------------
  db:
    image: postgres:17-alpine
    container_name: day-pilot-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-daypilot}
      POSTGRES_USER: ${POSTGRES_USER:-daypilot}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-daypilot}
    volumes:
      - db-data:/var/lib/postgresql/data
    networks:
      - daypilot-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-daypilot} -d ${POSTGRES_DB:-daypilot}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1G
        reservations:
          cpus: '0.25'
          memory: 256M

  # ---------------------------------------------------------------------------
  # Backend – FastAPI application
  #
  # Persistent state lives in PostgreSQL (events, todos, shopping list, family
  # profiles, settings, notification dedup markers). The /app/data volume still
  # holds the Google OAuth token and any legacy JSON files imported on first
  # boot.
  # ---------------------------------------------------------------------------
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: day-pilot-backend
    restart: unless-stopped
    env_file: .env
    environment:
      - DATABASE_URL=postgresql+psycopg://${POSTGRES_USER:-daypilot}:${POSTGRES_PASSWORD:-daypilot}@db:5432/${POSTGRES_DB:-daypilot}
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data        # Google OAuth token + legacy JSON import source
    depends_on:
      db:
        condition: service_healthy
    networks:
      - daypilot-net
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 512M
        reservations:
          cpus: '0.25'
          memory: 128M

  # ---------------------------------------------------------------------------
  # Frontend – React PWA served via nginx
  # ---------------------------------------------------------------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # Leave empty so the built JS uses a relative /api path (proxied by
        # nginx). Set VITE_API_URL in .env only when a split-origin setup is
        # needed (e.g. frontend and backend on different ports/hosts).
        VITE_API_URL: ${VITE_API_URL:-}
    container_name: day-pilot-frontend
    restart: unless-stopped
    ports:
      - "3000:80"
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - daypilot-net
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.1'
          memory: 64M

networks:
  daypilot-net:
    driver: bridge

volumes:
  db-data:
